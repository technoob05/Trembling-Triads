# PROJECT TRIAD - Requirements for Reproduction
# Python 3.10+ recommended

# Core Dependencies
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
scipy>=1.10.0

# LLM API Clients
openai>=1.0.0
anthropic>=0.18.0
mistralai>=0.1.0

# Deep Learning (Optional - only if using local models)
torch>=2.0.0
transformers>=4.30.0
accelerate>=0.20.0
bitsandbytes>=0.41.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.65.0

# Jupyter (Optional - for notebooks)
jupyter>=1.0.0
ipywidgets>=8.0.0

# Data Validation
jsonschema>=4.17.0

# Note: For local model inference (Qwen, Llama), install:
# pip install unsloth unsloth_zoo
# or use the official model providers' libraries
